# Cycle 6 — C1 R3F avatar shell

## Summary

- Added **`@react-three/fiber`**, **`three`**, and **`@types/three`**.
- **`components/avatar/ExecutiveAvatar.tsx`** — client `Canvas` with transparent clear, shadows, lights, capsule placeholder + ground shadow plane; labeled “Executive (preview)”.
- **`components/personas/PersonaChatSection.tsx`** — client wrapper: `dynamic(..., { ssr: false })` for the avatar (Next.js 16 forbids `ssr: false` `dynamic` in Server Components), loading skeleton, grid with **`ChatPanel`**.
- **`app/personas/[id]/chat/page.tsx`** — `max-w-5xl`, renders **`PersonaChatSection`** with `personaId`.

## Verification

- `pnpm lint` — PASS
- `pnpm build` — PASS

## Review gates

- **Spec (C1 plan):** PASS
- **Code review (scoped):** PASS — WebGL gated to client; no blocking issues noted.

## Follow-ups (C2)

- Replace capsule with glTF/VRM; viseme/lip-sync hook; optional `public/avatars/` asset.

## Note

- Next.js **16**: `next/dynamic` with `ssr: false` must live in a **Client Component**, not a Server Component page.
