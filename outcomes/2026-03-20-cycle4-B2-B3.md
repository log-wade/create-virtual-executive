# Cycle 4 — B2 `/api/tts` + B3 chat Speak

## Summary

- **B2:** Added `POST /api/tts` returning `audio/mpeg`, JSON body `{ text, personaId? }`, shared `MAX_TTS_INPUT_CHARS` limit, invalid JSON / config / provider error handling (`503` when ElevenLabs env missing).
- **B3:** Assistant bubbles show an optional **Speak** button (hidden while the latest reply is still streaming). Fetches TTS, plays via `Audio` + object URL with cleanup; handles play() rejection; amber banner for TTS errors.

## Files changed

- `app/api/tts/route.ts` (new)
- `components/ChatPanel.tsx` — TTS playback helpers + UI

## Verification

- `pnpm lint` — PASS
- `pnpm build` — PASS

## Middleware

- No change: existing `/api/:path*` matcher already rate-limits `/api/tts` when Upstash is configured.

## Review gates (this cycle)

- **Spec (B2/B3 plan):** PASS — limits match `lib/voice/tts.ts`, no autoplay, button-gated playback.
- **Code review (scoped):** PASS — no blocking issues; manual happy-path with real keys still recommended.

## Follow-ups

- B4: mic + `POST /api/stt` (or client SDK) and wire transcript into chat.
- Optional: per-persona `personaId` routing for voice selection (currently accepted but ignored for synthesis v1).
