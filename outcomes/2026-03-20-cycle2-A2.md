# Summary

Implemented Task A2 by adding a new non-stream executive helper for turn-based integrations and documenting when to use stream vs complete reply paths.

# Files Changed

- `lib/executive/complete-reply.ts` (new)
  - Added `completeExecutiveReply(personaId, messages): Promise<string>`.
  - Uses `getAnthropic`, `getAnthropicModel`, `buildChatSystemPrompt`, and `loadPersonaPackage`.
  - Uses `anthropic.messages.create` with the same message mapping convention as existing chat flow.
  - Extracts and concatenates text blocks from Anthropic response content.
- `lib/executive/stream-reply.ts`
  - Added concise comments clarifying stream helper usage for web chat and complete helper usage for voice v1.

# Verification Results

- `pnpm lint` -> PASS
  - `eslint` completed with exit code `0`.
- `pnpm build` -> PASS
  - `next build` completed with exit code `0`.
  - Key lines:
    - `Compiled successfully`
    - `Finished TypeScript`
    - `Generating static pages ...`
  - Note: existing warning observed: middleware convention deprecation (`middleware` -> `proxy`), unrelated to this task.

# Risks/Follow-ups

- `completeExecutiveReply` currently joins all text blocks and ignores non-text blocks, which is appropriate for v1 voice adapters but may need extension if tool-use blocks are introduced.
- If phone workers require deterministic sentence chunking for TTS latency tuning, add a chunking strategy at adapter level (keep helper generic).
