# Cycle 5 — B4 Mic + STT → chat

## Summary

- **`lib/voice/stt.ts`** — `transcribeAudio(buffer, contentType)` via Deepgram prerecorded `v1/listen` (`nova-2`, `smart_format=true`), `MAX_STT_AUDIO_BYTES` (5 MiB).
- **`app/api/stt/route.ts`** — `POST` `multipart/form-data` field `audio` (File), returns `{ transcript }`; 400/413/503/500 as appropriate; `maxDuration` 60s.
- **`components/ChatPanel.tsx`** — **Mic** / **Stop** with `MediaRecorder` (WebM/opus or mp4 fallback), upload blob, then same chat path as typed messages via shared `runChatTurn`. `messagesRef` for post-record snapshot; unmount skips upload. Mic disabled while chat/STT/TTS busy; **Stop** stays enabled while recording.

## Verification

- `pnpm lint` — PASS
- `pnpm build` — PASS

## Middleware

- `/api/stt` covered by existing `/api/:path*` rate limit when Upstash is set.

## Review gates (this cycle)

- **Spec (B4 plan):** PASS — server-side blob upload, chat integration, no required client Deepgram key.
- **Code review (scoped):** PASS — no blocking issues; live Deepgram + mic smoke test recommended.

## Follow-ups

- Phase **C1** — R3F avatar shell on chat page.
- Optional: longer recordings / streaming STT; model via `DEEPGRAM_MODEL` env later.
