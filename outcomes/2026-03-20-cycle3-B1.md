# Cycle 3 — B1 TTS scaffolding

## Summary

Implemented Phase B1: documented ElevenLabs + Deepgram env vars, added typed config accessors with fail-fast assertions, and a minimal server-only `synthesizeSpeech()` wrapper calling ElevenLabs text-to-speech API.

## Files changed

- `.env.example` — `ELEVENLABS_*`, `DEEPGRAM_API_KEY` (commented)
- `lib/config.ts` — getters + `assertElevenLabsTtsConfigured`, `assertDeepgramSttConfigured`
- `lib/voice/tts.ts` — `synthesizeSpeech`, `MAX_TTS_INPUT_CHARS`
- `README.md` — env table rows for voice keys

## Verification

- `pnpm lint` — PASS
- `pnpm build` — PASS

## Risks / follow-ups

- Live TTS requires valid keys and network; no automated integration test in this task.
- B2 should call `synthesizeSpeech` from `POST /api/tts` with matching length limits.

## Gates

- Spec (B1 plan): PASS
- Code review (scoped): PASS — no blocking issues identified
