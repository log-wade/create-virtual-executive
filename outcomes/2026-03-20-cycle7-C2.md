# Cycle 7 — C2 glTF + lip-sync stub

## Summary

- **Asset:** Bundled `public/avatars/robot-expressive.glb` (Three.js `RobotExpressive` example) + `public/avatars/README.md` (license/swap notes).
- **`lib/avatar/morph-lip-sync.ts`:** Collect morph targets whose names match mouth/jaw/viseme-style patterns, else `Surprised`, else first morph (fits RobotExpressive `Surprised` on head meshes).
- **`ExecutiveAvatar`:** `useLoader(GLTFLoader)` (no `@react-three/drei` — `pnpm add` hit **ENOSPC** on this machine). Cloned scene per load, auto-fit scale/position, shadows, `LipSyncDriver` + `useFrame` stub driven by `isSpeaking`.
- **Props / env:** `ExecutiveAvatarProps.modelUrl`, `NEXT_PUBLIC_AVATAR_MODEL_URL`, default `/avatars/robot-expressive.glb`. `useLoader.preload` for default URL.
- **TTS bridge:** `ChatPanel` `onTtsPlaybackChange(playing)` with **generation guard** so stale `onended` does not clear lip-sync mid–new clip. `PersonaChatSection` wires `avatarSpeaking` → `ExecutiveAvatar`.

## Verification

- `pnpm lint` — PASS
- `pnpm build` — PASS

## Review gates

- **Spec (C2):** PASS for glTF + stub + TTS coupling; **VRM** not implemented (follow-up if required).
- **Code review (scoped):** PASS — no blocking issues.

## Follow-ups

- Optional **`@react-three/drei`** when disk space allows (`Center`, `Stage`, helpers).
- **VRM** pipeline (`@pixiv/three-vrm` or similar) if Ready Player Me / VRM is required.
- **Real visemes:** wire ElevenLabs (or other) timing into `LipSyncDriver` instead of sine stub; optional **Web Audio** analyser for RMS-driven jaw.
